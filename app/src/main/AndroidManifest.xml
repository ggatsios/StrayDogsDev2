<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.straydogsdev">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StrayDogsDev"
        tools:targetApi="31">

        <!-- Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAfWM0G7G9jY3q18yAdvUOqCmjIBCTZLxA" />


        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:label="Register">
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:label ="Login">

        </activity>

        <!-- Main Menu Activity -->
        <activity
            android:name=".MainMenuActivity"
            android:label="Main Menu">

        </activity>

        <!-- report Dog Activity -->
        <activity
            android:name=".ReportDogActivity"
            android:label="Report a Dog">

        </activity>

        <!-- Search Dog Activity -->
        <activity
            android:name=".SearchDogActivity"
            android:label="Find Dogs">

        </activity>


    </application>

</manifest>
